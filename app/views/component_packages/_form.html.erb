<%= form_for(component_package) do |f| %>
  <% if component_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component_package.errors.count, "error") %> prohibited this component_package from being saved:</h2>

      <ul>
      <% component_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component_id %>
    <%= f.number_field :component_id %>
  </div>

  <div class="field">
    <%= f.label :package_id %>
    <%= f.number_field :package_id %>
  </div>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
