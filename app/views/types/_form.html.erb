<%= form_for(type, :html => {:class => 'form-horizontal'}) do |f| %>
  <% if type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>

  <div class="form-group">
   <label class="col-md-4 control-label" for="p_number">User Type</label>
      <div class="col-md-6">
        <%= f.text_field :user_type, :class => 'form-control input-md'%>
      </div>
    </div>
    <div class="form-group">
   <label class="col-md-4 control-label" for="p_number">User Description</label>
      <div class="col-md-6">
        <%= f.text_field :user_description, :class => 'form-control input-md'%>
      </div>
      </div>

  <div class="form-group">
      <div class="col-md-10 control-label">
        <%= f.submit 'Create Type', :class => 'btn btn-success'%>
      </div>
       </div>

   </fieldset>
<% end %>

